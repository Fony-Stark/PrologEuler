top_right = previous + i * 2 * 4
top_left = previous + i * 2 * 3 + (i - 1) * 2
bottom_right = previous + 1 * 2 + (i - 1) * 2 * 3
bottom_left = previous + i * 2 * 2 + (i - 1) * 2 * 2

result = top_left + top_right + bottom_left + bottom_right + 1 = 669171001